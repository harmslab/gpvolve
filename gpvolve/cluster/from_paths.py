from .utils import *
import msmtools.analysis as mana
import warnings


class ClustersFromPaths(object):
    """Take dictionary of sampled paths initialized from each node of the system and turn it into clusters"""
    def __init__(self, paths):
        self.paths

        pass